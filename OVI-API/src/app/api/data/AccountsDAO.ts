import {AccountType} from "../model/enum/AccountType";

export class AccountsDAO {
    // TODO
    public getAccountByUsername(username: string): string | null {
        console.warn("TODO");
        return null;
    }

    // TODO
    public saveAccount(type: AccountType, username: string, encryptedPassword: string) {
        console.warn("TODO");
    }
}